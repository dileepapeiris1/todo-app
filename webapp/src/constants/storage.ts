/** localStorage key constants — used by AuthContext, the API client, and the theme. */

/** Key under which the JWT is stored in localStorage. */
export const TOKEN_KEY = 'tracklog_token';

/** Key under which the serialised AuthUser object is stored in localStorage. */
export const USER_KEY = 'tracklog_user';

/** Key under which the chosen colour theme ('light' | 'dark') is stored in localStorage. */
export const THEME_KEY = 'tracklog_theme';
