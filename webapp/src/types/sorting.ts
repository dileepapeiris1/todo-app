/** Fields available for sorting the todo list. */
export enum SortField {
  CreatedAt = 'createdAt',
  DueDate   = 'dueDate',
  Title     = 'title',
}

/** Sort direction. */
export enum SortOrder {
  Asc  = 'asc',
  Desc = 'desc',
}
