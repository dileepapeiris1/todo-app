/** Default number of records to skip for offset-based pagination. */
export const DEFAULT_PAGINATION_OFFSET = 0;

/** Default maximum records returned per paginated response. */
export const DEFAULT_PAGINATION_LIMIT = 10;
